fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
jinja2>=3.1.0
aiofiles>=23.1.0
python-multipart>=0.0.6
websockets>=12.0
yfinance>=0.2.36
pandas>=2.0.0
PyJWT>=2.8.0
cryptography>=41.0.0
Pillow>=10.1.0   # 10.1 added a SIZE argument to load_default();
                 # without it the server has no scalable font at all
# Server-side PDF generation for one-click tear sheet downloads.
# reportlab = pure Python, no system deps (works on Railway nixpacks).
reportlab>=4.0.0
matplotlib>=3.7.0
# HTML parsing for the in-app SEC-document → PDF converter (doc_pdf.py).
# Both are also pulled in transitively by yfinance; pinned here to be explicit.
beautifulsoup4>=4.12.0
lxml>=4.9.0
